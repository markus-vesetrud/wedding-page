services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wedding-page
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - STATE_FILE=/data/state.json
    volumes:
      - ./data:/data
    restart: unless-stopped
