services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wedding-page
    ports:
      - "2718:3000"
    environment:
      - HOST_PORT=2718
      - PORT=3000
      - STATE_DIR=/data
    volumes:
      - ./data:/data
    restart: unless-stopped
